// packages
//import { useEffect } from 'react';
// contexts
//import { useTodoList } from '../contexts/todo.context';

export const Note = () => {
	/*const { todoList, setTodoList, isLoading, error } = useTodoList();

	useEffect(() => {
		console.log(isLoading, error)
	}, [isLoading, error]);

	useEffect(() => {
		console.log(todoList)
	}, [todoList, setTodoList]);*/

	return (
		<div className='nm-note nm-bg-o'>
			<div className='nm-notes'>
				items
			</div>
		</div>
	);
}